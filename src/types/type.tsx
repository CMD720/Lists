export type TModal = {
    modalWindow: boolean
}

export type TListItem = {
    item: string;
    id: string;
}

export type TEmailId = {
    login: string
}

export type TLists = {
    Name: string;
    List: TListItem;
    id: string;
}
